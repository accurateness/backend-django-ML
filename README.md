# djangobackend

In this repository, the code for an application based on Django is kept. The application talks with a machine learning model based on Flask API as well as a frontend based on React.js.